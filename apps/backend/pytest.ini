[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

markers =
    unit: isolated tests — no external services, no real DB (fast)
    integration: tests that touch real files or external services (slower)
